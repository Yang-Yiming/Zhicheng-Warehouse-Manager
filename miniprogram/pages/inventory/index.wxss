.page { background: #f5f5f5; min-height: 100vh; }
.search-bar { padding: 16rpx 24rpx; background: #fff; }
.search-input { background: #f0f0f0; border-radius: 32rpx; padding: 12rpx 28rpx; font-size: 28rpx; width: 100%; box-sizing: border-box; }
.list { padding: 16rpx; }
.card { background: #fff; border-radius: 16rpx; padding: 24rpx; margin-bottom: 16rpx; }
.card-row { display: flex; align-items: center; margin-bottom: 10rpx; }
.card-row:last-child { margin-bottom: 0; }
.main-row { gap: 12rpx; }
.item-name { font-size: 32rpx; font-weight: 600; flex: 1; color: #222; }
.qty-badge { display: flex; align-items: baseline; gap: 4rpx; background: #e8f5e9; border-radius: 12rpx; padding: 6rpx 16rpx; }
.qty-num { font-size: 32rpx; font-weight: 700; color: #2e7d32; }
.qty-unit { font-size: 22rpx; color: #4caf50; }
.sub-row { gap: 8rpx; flex-wrap: wrap; }
.sub { font-size: 24rpx; color: #666; }
.sep { font-size: 24rpx; color: #ccc; }
.muted { color: #999; }
.unauthorized-tip { display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 120rpx 48rpx; gap: 24rpx; }
.unauthorized-text { font-size: 30rpx; color: #999; text-align: center; }
.tip { text-align: center; padding: 48rpx; color: #999; font-size: 28rpx; }
.fab {
  position: fixed;
  right: 40rpx;
  bottom: 60rpx;
  width: 100rpx;
  height: 100rpx;
  border-radius: 50rpx;
  background: #07c160;
  color: #fff;
  font-size: 56rpx;
  line-height: 100rpx;
  text-align: center;
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.18);
  z-index: 100;
}
